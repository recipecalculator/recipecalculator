<Page
    x:Class="RecipeCalculator.CalculationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RecipeCalculator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton x:Name="CalculateButton" x:Uid="Calculate" Icon="Calculator" Click="CalculateButton_Click"/>
        </CommandBar>
    </Page.BottomAppBar>

    <Pivot Title="Pivot">
        <PivotItem x:Name="Portions" x:Uid="Portions">
            <StackPanel>
                <TextBox x:Name="OrigPortionsTB" x:Uid="OrigPortions" Margin="0" TextWrapping="Wrap" VerticalAlignment="Stretch" InputScope="Number"/>
                <TextBox x:Name="NewPortionsTB" x:Uid="NewPortions" Margin="0" TextWrapping="Wrap" VerticalAlignment="Stretch" InputScope="Number"/>
            </StackPanel>
        </PivotItem>
        <PivotItem x:Name="Ingredients" x:Uid="Ingredients">
            <Grid/>
        </PivotItem>
    </Pivot>
</Page>
